
import { environment } from '../environments/environment';

export const URLS = {
    LOGIN: environment.base + '/sessions',
    REGISTER: environment.base + '/register',
    PROJECTS: environment.base + '/projects'
};
